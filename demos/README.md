# Captured compiler output — live-demo fallbacks

Real output from the real compilers, captured by `tools/capture-demos.sh`.
Rendered on each demo slide in a pane that stays hidden unless the live edit
fails. Regenerate after any change to the example code.

Each demo's edit is recorded in the capture script — that script is the
authoritative description of what to type on stage, because it is executed.
