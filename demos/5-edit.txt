$ sed -i '…ch5.send(captured) → ch5.receive()._2…' PaymentDemo.scala
$ git diff -- PaymentDemo.scala
  @@ -138,7 +138,7 @@
       val authorized        = authorize(order, ReviewerApproved(approval))
       val ch5               = ch4.send(authorized)
       val captured          = capture(authorized)
  -    ch5.send(captured)
  +    ch5.receive()._2
   
     // ─── Client handlers ────────────────────────────────────────────────────────
   
